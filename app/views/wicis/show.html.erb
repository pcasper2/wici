<br>
<div class="row">
  <div class="col-md-12">
    <div class="panel panel-primary">
      <div class="panel panel-heading">
        <center><h4><%= markdown_to_html @wici.title %></h4></center>
      </div>
      <div class="panel panel-body">
        <%= markdown_to_html @wici.body %>
      </div>
    </div>
  </div>
  <% if policy(@wici).edit? %>
    <div class="col-md-8">
      <%= link_to "Edit", edit_wici_path(@wici), class: "btn btn-primary" %>
    </div>
  <% end %>

</div>
