<% if current_user.role == 'premium' || current_user.role == "admin" %>
<%= form_for @wici do |f| %>
  <div class="form-group">
    <%= f.label :private, class: 'checkbox' do %>
      <%= f.check_box :private%> Private Wici
    <% end %>
  </div>
<% end %>
<% end %>

